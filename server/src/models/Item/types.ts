export interface IData extends IProps {
  name: string;
  collectionId: string;
}

export interface IProps {
  strProps: string;
  boolProps: string;
  intProps: string;
  textProps: string;
  dateProps: string;
}
