import {theme} from '../../styles/theme';

export const styles = {
  card: {
    position: 'relative',
  },

  img: {
    height: '342px',
    width: '100%',
    backgroundColor: theme.palette.fourth.light,
    objectFit: 'cover',
  },
};
